# TypeScript 项目模板

这是一个从零搭建的完整 TypeScript 项目，包含了现代 TypeScript 开发所需的所有配置和工具。

## 🚀 特性

- ✅ **TypeScript 5.0+** - 最新的 TypeScript 特性
- ✅ **ESLint** - 代码质量检查
- ✅ **Prettier** - 代码格式化
- ✅ **Jest** - 单元测试框架
- ✅ **ts-node** - 直接运行 TypeScript 文件
- ✅ **nodemon** - 开发时自动重启
- ✅ **严格模式** - 完整的 TypeScript 类型检查

## 📁 项目结构

```
src/
├── types/           # 类型定义
│   └── User.ts     # 用户相关类型
├── services/        # 业务服务层
│   └── UserService.ts # 用户服务
├── utils/           # 工具类
│   └── Logger.ts   # 日志工具
├── test/            # 测试文件
│   ├── setup.ts    # 测试设置
│   └── UserService.test.ts # 用户服务测试
└── index.ts        # 主入口文件
```

## 🛠️ 安装依赖

```bash
# 使用 pnpm 安装依赖
pnpm install
```

## 📝 可用的脚本命令

### 开发相关
```bash
# 开发模式运行（直接运行 TypeScript）
pnpm dev

# 开发模式运行（监听文件变化）
pnpm dev:watch

# 构建项目
pnpm build

# 构建项目（监听模式）
pnpm build:watch
```

### 测试相关
```bash
# 运行测试
pnpm test

# 运行测试（监听模式）
pnpm test:watch
```

### 代码质量
```bash
# 代码检查
pnpm lint

# 自动修复代码问题
pnpm lint:fix

# 代码格式化
pnpm format
```

### 其他
```bash
# 清理构建文件
pnpm clean

# 运行构建后的代码
pnpm start
```

## ⚙️ 配置说明

### TypeScript 配置 (tsconfig.json)
- 目标版本：ES2020
- 严格模式：启用
- 输出目录：`./dist`
- 源码目录：`./src`
- 生成声明文件和 source map

### ESLint 配置 (.eslintrc.js)
- 使用 TypeScript 解析器
- 集成 Prettier
- 严格的 TypeScript 规则
- 代码质量检查

### Prettier 配置 (.prettierrc)
- 单引号
- 尾随逗号
- 80 字符宽度
- 2 空格缩进

### Jest 配置 (jest.config.js)
- TypeScript 支持
- 代码覆盖率
- 测试环境：Node.js

## 🔧 开发工作流

1. **编写代码** - 在 `src/` 目录下编写 TypeScript 代码
2. **类型检查** - TypeScript 编译器会进行类型检查
3. **代码检查** - ESLint 检查代码质量
4. **代码格式化** - Prettier 自动格式化代码
5. **运行测试** - Jest 运行单元测试
6. **构建项目** - 编译 TypeScript 到 JavaScript

## 📚 示例代码

项目包含了一个完整的用户管理系统示例：

- **类型定义** - 用户接口和参数类型
- **服务层** - 用户 CRUD 操作
- **工具类** - 日志记录器
- **测试用例** - 完整的单元测试

## 🚨 注意事项

- 确保使用 Node.js 16+ 版本
- 推荐使用 pnpm 作为包管理器
- 开发时建议使用 VS Code + TypeScript 插件
- 提交代码前请运行 `pnpm lint` 和 `pnpm test`

## 🔗 相关链接

- [TypeScript 官方文档](https://www.typescriptlang.org/)
- [ESLint 官方文档](https://eslint.org/)
- [Prettier 官方文档](https://prettier.io/)
- [Jest 官方文档](https://jestjs.io/)

## 📄 许可证

ISC 